/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // Ensuring the zinc and emerald shades are properly mapped
        zinc: {
          950: '#09090b',
        },
      },
      // This ensures your animations (like the heartbeat) work with Tailwind
      animation: {
        'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
      }
    },
  },
  plugins: [],
}